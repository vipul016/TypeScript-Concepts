// object shapes
// union types


type Person1 = {
    id : number,
    address : string,
    salary : number
}

const vipul12 : Person1 = {
    id : 1,
    address: "g2",
    salary :10
}

type Status = 'new' | 'paid' | 'pending'

